schema: "./src/**/*.graphql" # GraphQL types (input file)
generates:
  ./src/graphql/gql-types.ts: # Typescript types (output generated file)
    plugins: # List of needed plugins (installed as devDeps)
      - typescript
